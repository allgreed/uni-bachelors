import sys


def main():
    mode_flag = None
    strategy_flag = None

    if ["-e"] in sys.argv[1:]:
        mode = # embed
    else if ["-d"] in sys.argv[1:]:
        mode = # decrypt
    else:
        # TODO: raise
        raise hell

    if ["-1"] in sys.argv[1:]:
        strategy = 
    if ["-2"] in sys.argv[1:]:
        strategy = 
    if ["-3"] in sys.argv[1:]:
        strategy = 
    if ["-4"] in sys.argv[1:]:
        strategy = 
    else:
        # TODO: raise
        raise hell

    # read mess if needed
    # read cover
    # read watermark if needed

    # apply strategy with the right action
        # write
        # remove arbitrary occurences
        # apply
        # save as watermark

        # read
        # save as detect.txt

if __name__ == "__main__":
    main()
